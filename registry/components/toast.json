{
  "name": "toast",
  "title": "Toast",
  "description": "Tijdelijke melding met toon, actie en automatische sluiting.",
  "category": "Overlays",
  "dependencies": [],
  "files": [
    {
      "path": "components/toast.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"../lib/cn\";\nimport { Portal } from \"../lib/portal\";\nimport { Icon, type IconName } from \"../icons/icon\";\n\nexport type ToastTone = \"neutral\" | \"accent\" | \"green\" | \"amber\" | \"red\" | \"blue\";\n\nexport interface ToastOptions {\n  title: React.ReactNode;\n  description?: React.ReactNode;\n  tone?: ToastTone;\n  /** Duur in ms; 0 blijft staan tot de gebruiker sluit. */\n  duration?: number;\n  icon?: React.ReactNode;\n  action?: { label: string; onClick: () => void };\n}\n\nexport interface ToastItem extends ToastOptions {\n  id: string;\n}\n\ninterface ToastContextValue {\n  toasts: ToastItem[];\n  push: (options: ToastOptions) => string;\n  dismiss: (id: string) => void;\n}\n\nconst ToastContext = React.createContext<ToastContextValue | null>(null);\n\nexport interface ToastProviderProps {\n  children: React.ReactNode;\n  /** Maximum aantal zichtbare meldingen. */\n  max?: number;\n  position?: \"bottom-right\" | \"bottom-left\" | \"top-right\" | \"top-left\" | \"bottom-center\" | \"top-center\";\n  /** Standaardduur in ms. */\n  duration?: number;\n}\n\n/** ToastProvider — zet dit één keer rond je app. */\nexport function ToastProvider({\n  children,\n  max = 4,\n  position = \"bottom-right\",\n  duration = 4200,\n}: ToastProviderProps) {\n  const [toasts, setToasts] = React.useState<ToastItem[]>([]);\n  const timers = React.useRef(new Map<string, number>());\n\n  const dismiss = React.useCallback((id: string) => {\n    setToasts((prev) => prev.filter((toast) => toast.id !== id));\n    const timer = timers.current.get(id);\n    if (timer) window.clearTimeout(timer);\n    timers.current.delete(id);\n  }, []);\n\n  const push = React.useCallback(\n    (options: ToastOptions) => {\n      const id = `pxui-toast-${Math.random().toString(36).slice(2, 10)}`;\n      setToasts((prev) => [...prev, { ...options, id }].slice(-max));\n      const life = options.duration ?? duration;\n      if (life > 0) {\n        timers.current.set(id, window.setTimeout(() => dismiss(id), life));\n      }\n      return id;\n    },\n    [max, duration, dismiss]\n  );\n\n  React.useEffect(() => {\n    const map = timers.current;\n    return () => map.forEach((timer) => window.clearTimeout(timer));\n  }, []);\n\n  return (\n    <ToastContext.Provider value={{ toasts, push, dismiss }}>\n      {children}\n      <Toaster position={position} />\n    </ToastContext.Provider>\n  );\n}\n\nconst TONE_ICON: Record<ToastTone, IconName> = {\n  neutral: \"info\",\n  accent: \"sparkles\",\n  green: \"checkCircle\",\n  amber: \"alert\",\n  red: \"alertCircle\",\n  blue: \"info\",\n};\n\n/** Toaster — rendert de stapel meldingen. Wordt door ToastProvider geplaatst. */\nexport function Toaster({ position = \"bottom-right\" }: { position?: ToastProviderProps[\"position\"] }) {\n  const context = React.useContext(ToastContext);\n  if (!context || context.toasts.length === 0) return null;\n\n  return (\n    <Portal>\n      <div className={cn(\"pxui-toaster\", `pxui-toaster-${position}`)} role=\"region\" aria-label=\"Meldingen\">\n        {context.toasts.map((toast) => {\n          const tone = toast.tone ?? \"neutral\";\n          return (\n            <div key={toast.id} className={cn(\"pxui-toast\", `pxui-toast-${tone}`)} role=\"status\">\n              <span className=\"pxui-toast-icon\">\n                {toast.icon ?? <Icon name={TONE_ICON[tone]} size={15} />}\n              </span>\n              <div className=\"pxui-toast-body\">\n                <div className=\"pxui-toast-title\">{toast.title}</div>\n                {toast.description && <div className=\"pxui-toast-description\">{toast.description}</div>}\n              </div>\n              {toast.action && (\n                <button\n                  type=\"button\"\n                  className=\"pxui-toast-action\"\n                  onClick={() => {\n                    toast.action?.onClick();\n                    context.dismiss(toast.id);\n                  }}\n                >\n                  {toast.action.label}\n                </button>\n              )}\n              <button\n                type=\"button\"\n                className=\"pxui-toast-close\"\n                aria-label=\"Sluiten\"\n                onClick={() => context.dismiss(toast.id)}\n              >\n                <Icon name=\"x\" size={14} />\n              </button>\n            </div>\n          );\n        })}\n      </div>\n    </Portal>\n  );\n}\n\nexport interface ToastApi {\n  (options: ToastOptions): string;\n  success: (title: React.ReactNode, options?: Omit<ToastOptions, \"title\" | \"tone\">) => string;\n  error: (title: React.ReactNode, options?: Omit<ToastOptions, \"title\" | \"tone\">) => string;\n  warning: (title: React.ReactNode, options?: Omit<ToastOptions, \"title\" | \"tone\">) => string;\n  info: (title: React.ReactNode, options?: Omit<ToastOptions, \"title\" | \"tone\">) => string;\n  dismiss: (id: string) => void;\n}\n\n/** useToast — meldingen tonen vanuit elk component onder de ToastProvider. */\nexport function useToast(): ToastApi {\n  const context = React.useContext(ToastContext);\n  if (!context) throw new Error(\"useToast() vereist een <ToastProvider> hoger in de boom.\");\n\n  return React.useMemo(() => {\n    const api = ((options: ToastOptions) => context.push(options)) as ToastApi;\n    api.success = (title, options) => context.push({ ...options, title, tone: \"green\" });\n    api.error = (title, options) => context.push({ ...options, title, tone: \"red\" });\n    api.warning = (title, options) => context.push({ ...options, title, tone: \"amber\" });\n    api.info = (title, options) => context.push({ ...options, title, tone: \"blue\" });\n    api.dismiss = context.dismiss;\n    return api;\n  }, [context]);\n}\n"
    },
    {
      "path": "components/toast.css",
      "content": "/* ============================ Toast ============================ */\n.pxui-toaster {\n  position: fixed; z-index: var(--z-toast);\n  display: flex; flex-direction: column; gap: 10px;\n  pointer-events: none; max-width: min(380px, calc(100vw - 24px));\n}\n.pxui-toaster-bottom-right { bottom: 24px; right: 24px; }\n.pxui-toaster-bottom-left { bottom: 24px; left: 24px; }\n.pxui-toaster-top-right { top: 24px; right: 24px; flex-direction: column-reverse; }\n.pxui-toaster-top-left { top: 24px; left: 24px; flex-direction: column-reverse; }\n.pxui-toaster-bottom-center { bottom: 24px; left: 50%; transform: translateX(-50%); }\n.pxui-toaster-top-center { top: 24px; left: 50%; transform: translateX(-50%); flex-direction: column-reverse; }\n\n.pxui-toast {\n  --pxui-toast-color: var(--text-3);\n  pointer-events: auto; position: relative;\n  display: flex; align-items: flex-start; gap: 11px;\n  min-width: 280px; padding: 13px 14px;\n  background: var(--surface); border: 1px solid var(--border);\n  border-radius: var(--r-md); box-shadow: var(--sh-lg);\n  animation: pxui-toast-in var(--dur-slow) var(--ease-pop) both;\n}\n@keyframes pxui-toast-in {\n  from { opacity: 0; transform: translateY(14px) scale(.96); }\n  to { opacity: 1; transform: none; }\n}\n\n.pxui-toast-accent { --pxui-toast-color: var(--accent); }\n.pxui-toast-green { --pxui-toast-color: var(--green); }\n.pxui-toast-amber { --pxui-toast-color: var(--amber); }\n.pxui-toast-red { --pxui-toast-color: var(--red); }\n.pxui-toast-blue { --pxui-toast-color: var(--blue); }\n\n.pxui-toast-icon {\n  display: grid; place-items: center; flex: none;\n  width: 26px; height: 26px; border-radius: 7px;\n  color: var(--pxui-toast-color);\n  background: color-mix(in srgb, var(--pxui-toast-color) 14%, transparent);\n}\n.pxui-toast-body { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 2px; padding-top: 2px; }\n.pxui-toast-title { font-size: 13.5px; font-weight: 600; color: var(--text); line-height: 1.35; }\n.pxui-toast-description { font-size: var(--fs-sm); color: var(--text-2); line-height: 1.45; }\n.pxui-toast-action {\n  flex: none; align-self: center; border: none; background: transparent;\n  color: var(--accent); font: inherit; font-size: 13px; font-weight: 650;\n  cursor: pointer; padding: 4px 6px; border-radius: var(--r-xs);\n}\n.pxui-toast-action:hover { background: var(--accent-tint); }\n.pxui-toast-close {\n  flex: none; display: grid; place-items: center; width: 22px; height: 22px;\n  border: none; background: transparent; color: var(--text-3);\n  border-radius: var(--r-xs); cursor: pointer;\n}\n.pxui-toast-close:hover { background: var(--surface-3); color: var(--text); }\n\n@media (max-width: 560px) {\n  .pxui-toaster { left: 12px; right: 12px; max-width: none; transform: none; }\n  .pxui-toaster-bottom-right, .pxui-toaster-bottom-left, .pxui-toaster-bottom-center { bottom: 12px; }\n  .pxui-toaster-top-right, .pxui-toaster-top-left, .pxui-toaster-top-center { top: 12px; }\n  .pxui-toast { min-width: 0; }\n}\n"
    }
  ]
}
