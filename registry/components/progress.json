{
  "name": "progress",
  "title": "Progress",
  "description": "Voortgangsbalk en ronde variant, ook onbepaald.",
  "category": "Data",
  "dependencies": [],
  "files": [
    {
      "path": "components/progress.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"../lib/cn\";\n\nexport interface ProgressProps extends React.HTMLAttributes<HTMLDivElement> {\n  /** Waarde tussen 0 en `max`. Laat weg voor een onbepaalde balk. */\n  value?: number;\n  max?: number;\n  tone?: \"accent\" | \"green\" | \"amber\" | \"red\" | \"blue\";\n  size?: \"sm\" | \"md\" | \"lg\";\n  /** Label boven de balk. */\n  label?: React.ReactNode;\n  /** Toont het percentage rechtsboven. */\n  showValue?: boolean;\n}\n\n/** Progress — voortgangsbalk, bepaald of onbepaald. */\nexport const Progress = React.forwardRef<HTMLDivElement, ProgressProps>(function Progress(\n  { value, max = 100, tone = \"accent\", size = \"md\", label, showValue, className, ...rest },\n  ref\n) {\n  const indeterminate = value === undefined || value === null;\n  const percent = indeterminate ? 0 : Math.min(100, Math.max(0, (value / max) * 100));\n\n  return (\n    <div ref={ref} className={cn(\"pxui-progress-wrap\", className)} {...rest}>\n      {(label || showValue) && (\n        <div className=\"pxui-progress-head\">\n          {label && <span className=\"pxui-progress-label\">{label}</span>}\n          {showValue && !indeterminate && (\n            <span className=\"pxui-progress-value\">{Math.round(percent)}%</span>\n          )}\n        </div>\n      )}\n      <div\n        className={cn(\"pxui-progress\", `pxui-progress-${size}`, `pxui-progress-${tone}`)}\n        role=\"progressbar\"\n        aria-valuemin={0}\n        aria-valuemax={max}\n        aria-valuenow={indeterminate ? undefined : value}\n      >\n        <div\n          className={cn(\"pxui-progress-bar\", indeterminate && \"pxui-progress-bar-indeterminate\")}\n          style={indeterminate ? undefined : { width: `${percent}%` }}\n        />\n      </div>\n    </div>\n  );\n});\n\nexport interface ProgressCircleProps extends React.HTMLAttributes<HTMLDivElement> {\n  value?: number;\n  max?: number;\n  size?: number;\n  thickness?: number;\n  tone?: \"accent\" | \"green\" | \"amber\" | \"red\" | \"blue\";\n  /** Toont het percentage in het midden. */\n  showValue?: boolean;\n}\n\n/** ProgressCircle — ronde voortgangsindicator. */\nexport const ProgressCircle = React.forwardRef<HTMLDivElement, ProgressCircleProps>(\n  function ProgressCircle(\n    { value = 0, max = 100, size = 56, thickness = 5, tone = \"accent\", showValue, className, ...rest },\n    ref\n  ) {\n    const percent = Math.min(100, Math.max(0, (value / max) * 100));\n    const radius = (size - thickness) / 2;\n    const circumference = 2 * Math.PI * radius;\n\n    return (\n      <div\n        ref={ref}\n        className={cn(\"pxui-progress-circle\", `pxui-progress-${tone}`, className)}\n        style={{ width: size, height: size }}\n        role=\"progressbar\"\n        aria-valuemin={0}\n        aria-valuemax={max}\n        aria-valuenow={value}\n        {...rest}\n      >\n        <svg width={size} height={size}>\n          <circle\n            className=\"pxui-progress-circle-track\"\n            cx={size / 2}\n            cy={size / 2}\n            r={radius}\n            strokeWidth={thickness}\n            fill=\"none\"\n          />\n          <circle\n            className=\"pxui-progress-circle-bar\"\n            cx={size / 2}\n            cy={size / 2}\n            r={radius}\n            strokeWidth={thickness}\n            fill=\"none\"\n            strokeLinecap=\"round\"\n            strokeDasharray={circumference}\n            strokeDashoffset={circumference - (percent / 100) * circumference}\n            transform={`rotate(-90 ${size / 2} ${size / 2})`}\n          />\n        </svg>\n        {showValue && <span className=\"pxui-progress-circle-value\">{Math.round(percent)}%</span>}\n      </div>\n    );\n  }\n);\n"
    },
    {
      "path": "components/progress.css",
      "content": "/* ============================ Progress ============================ */\n.pxui-progress-wrap { --pxui-progress-color: var(--accent); display: flex; flex-direction: column; gap: 7px; width: 100%; }\n.pxui-progress-head { display: flex; align-items: center; justify-content: space-between; gap: 12px; }\n.pxui-progress-label { font-size: 13px; font-weight: 550; color: var(--text-2); }\n.pxui-progress-value { font-size: var(--fs-sm); font-weight: 650; color: var(--text); font-family: var(--mono); }\n\n.pxui-progress {\n  --pxui-progress-color: var(--accent);\n  position: relative; width: 100%; height: 8px; overflow: hidden;\n  background: var(--surface-3); border-radius: var(--r-full);\n}\n.pxui-progress-sm { height: 5px; }\n.pxui-progress-lg { height: 12px; }\n\n.pxui-progress-accent { --pxui-progress-color: var(--accent); }\n.pxui-progress-green { --pxui-progress-color: var(--green); }\n.pxui-progress-amber { --pxui-progress-color: var(--amber); }\n.pxui-progress-red { --pxui-progress-color: var(--red); }\n.pxui-progress-blue { --pxui-progress-color: var(--blue); }\n\n.pxui-progress-bar {\n  height: 100%; border-radius: inherit;\n  background: var(--pxui-progress-color);\n  transition: width .45s var(--ease-out);\n}\n.pxui-progress-bar-indeterminate {\n  width: 38%; position: absolute; left: 0;\n  animation: pxui-progress-slide 1.15s var(--ease) infinite;\n}\n@keyframes pxui-progress-slide {\n  0% { transform: translateX(-100%); }\n  100% { transform: translateX(365%); }\n}\n\n/* ---------- Circle ---------- */\n.pxui-progress-circle { position: relative; display: inline-grid; place-items: center; }\n.pxui-progress-circle svg { position: absolute; inset: 0; }\n.pxui-progress-circle-track { stroke: var(--surface-3); }\n.pxui-progress-circle-bar {\n  stroke: var(--pxui-progress-color, var(--accent));\n  transition: stroke-dashoffset .5s var(--ease-out);\n}\n.pxui-progress-circle-value {\n  font-size: var(--fs-sm); font-weight: 700; color: var(--text); font-family: var(--mono);\n}\n"
    }
  ]
}
