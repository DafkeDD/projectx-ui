{
  "name": "switch",
  "title": "Switch",
  "description": "Aan/uit-schakelaar met label en beschrijving.",
  "category": "Formulieren",
  "dependencies": [],
  "files": [
    {
      "path": "components/switch.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"../lib/cn\";\nimport { useControllableState } from \"../lib/hooks\";\n\nexport interface SwitchProps extends Omit<React.ButtonHTMLAttributes<HTMLButtonElement>, \"onChange\" | \"value\"> {\n  checked?: boolean;\n  defaultChecked?: boolean;\n  onCheckedChange?: (checked: boolean) => void;\n  label?: React.ReactNode;\n  description?: React.ReactNode;\n  size?: \"sm\" | \"md\";\n  /** Label links van de schakelaar in plaats van rechts. */\n  labelPosition?: \"left\" | \"right\";\n}\n\n/** Switch — aan/uit-schakelaar (role=\"switch\"). */\nexport const Switch = React.forwardRef<HTMLButtonElement, SwitchProps>(function Switch(\n  {\n    checked,\n    defaultChecked = false,\n    onCheckedChange,\n    label,\n    description,\n    size = \"md\",\n    labelPosition = \"right\",\n    className,\n    disabled,\n    ...rest\n  },\n  ref\n) {\n  const [on, setOn] = useControllableState<boolean>({\n    value: checked,\n    defaultValue: defaultChecked,\n    onChange: onCheckedChange,\n  });\n\n  const control = (\n    <button\n      ref={ref}\n      type=\"button\"\n      role=\"switch\"\n      aria-checked={on}\n      disabled={disabled}\n      data-state={on ? \"on\" : \"off\"}\n      className={cn(\"pxui-switch\", `pxui-switch-${size}`, !label && !description && className)}\n      onClick={(event) => {\n        setOn(!on);\n        rest.onClick?.(event);\n      }}\n      {...rest}\n    >\n      <span className=\"pxui-switch-thumb\" />\n    </button>\n  );\n\n  if (!label && !description) return control;\n\n  return (\n    <label className={cn(\"pxui-switch-row\", labelPosition === \"left\" && \"pxui-switch-row-reverse\", disabled && \"pxui-switch-row-disabled\", className)}>\n      {control}\n      <span className=\"pxui-switch-text\">\n        {label && <span className=\"pxui-switch-label\">{label}</span>}\n        {description && <span className=\"pxui-switch-description\">{description}</span>}\n      </span>\n    </label>\n  );\n});\n"
    },
    {
      "path": "components/switch.css",
      "content": "/* ============================ Switch ============================ */\n.pxui-switch {\n  position: relative; width: 40px; height: 23px; flex: none; padding: 0;\n  border: none; border-radius: var(--r-full); background: var(--border-strong);\n  cursor: pointer; transition: background var(--dur);\n}\n.pxui-switch:focus-visible { outline: none; box-shadow: var(--ring); }\n.pxui-switch:disabled { opacity: .5; cursor: not-allowed; }\n.pxui-switch-thumb {\n  position: absolute; top: 2.5px; left: 2.5px;\n  width: 18px; height: 18px; border-radius: var(--r-full);\n  background: var(--surface); box-shadow: var(--sh-sm);\n  transition: transform var(--dur) var(--ease);\n}\n.pxui-switch[data-state=\"on\"] { background: var(--accent); }\n.pxui-switch[data-state=\"on\"] .pxui-switch-thumb { transform: translateX(17px); }\n\n.pxui-switch-sm { width: 32px; height: 19px; }\n.pxui-switch-sm .pxui-switch-thumb { width: 14px; height: 14px; top: 2.5px; left: 2.5px; }\n.pxui-switch-sm[data-state=\"on\"] .pxui-switch-thumb { transform: translateX(13px); }\n\n.pxui-switch-row { display: flex; align-items: flex-start; gap: 11px; cursor: pointer; }\n.pxui-switch-row-reverse { flex-direction: row-reverse; justify-content: space-between; }\n.pxui-switch-row-disabled { cursor: not-allowed; opacity: .6; }\n.pxui-switch-text { display: flex; flex-direction: column; gap: 2px; min-width: 0; }\n.pxui-switch-label { font-size: 13.5px; font-weight: 550; color: var(--text); line-height: 1.35; }\n.pxui-switch-description { font-size: var(--fs-sm); color: var(--text-3); line-height: 1.45; }\n"
    }
  ]
}
