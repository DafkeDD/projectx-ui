{
  "name": "swimlanes",
  "title": "Swimlanes",
  "description": "De agenda op zijn kant: een baan per resource, tijd van links naar rechts.",
  "category": "Datum & planning",
  "dependencies": [],
  "files": [
    {
      "path": "components/swimlanes.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"../lib/cn\";\nimport { formatTime } from \"../lib/date\";\nimport {\n  type ScheduleEvent,\n  type ScheduleResource,\n  countBookable,\n  groupByResource,\n  nowMinutes,\n} from \"../lib/schedule\";\n\nexport interface SwimlanesProps extends React.HTMLAttributes<HTMLDivElement> {\n  /** Rijen: artsen, kamers, machines … */\n  resources: ScheduleResource[];\n  events: ScheduleEvent[];\n  /** Eerste zichtbare uur. */\n  startHour?: number;\n  /** Laatste zichtbare uur. */\n  endHour?: number;\n  /** Breedte van één minuut in pixels. */\n  pxPerMinute?: number;\n  /** Hoogte van een baan. */\n  laneHeight?: number;\n  /** Breedte van de vaste kolom links. */\n  labelWidth?: number;\n  /** Verticale lijn op de huidige tijd. */\n  nowIndicator?: boolean;\n  /** Voor tests: vaste \"nu\"-tijd in minuten. */\n  now?: number;\n  emptyLabel?: React.ReactNode;\n  countLabel?: string;\n  onEventClick?: (event: ScheduleEvent) => void;\n  /** Klik op een leeg stuk van een baan. */\n  onSlotClick?: (resource: string, start: number) => void;\n  /** Rooster waarop een klik vastklikt, in minuten. */\n  step?: number;\n  renderEvent?: (event: ScheduleEvent) => React.ReactNode;\n}\n\n/**\n * Swimlanes — de agenda op zijn kant: één baan per resource, tijd loopt van\n * links naar rechts. Handig om in één oogopslag gaten en overlappingen tussen\n * mensen of ruimtes te zien.\n */\nexport const Swimlanes = React.forwardRef<HTMLDivElement, SwimlanesProps>(function Swimlanes(\n  {\n    resources,\n    events,\n    startHour = 8,\n    endHour = 18,\n    pxPerMinute = 2.4,\n    laneHeight = 54,\n    labelWidth = 180,\n    nowIndicator = true,\n    now,\n    emptyLabel = \"— vrij —\",\n    countLabel = \"afspraken\",\n    onEventClick,\n    onSlotClick,\n    step = 15,\n    renderEvent,\n    className,\n    style,\n    ...rest\n  },\n  ref\n) {\n  const dayStart = startHour * 60;\n  const dayEnd = endHour * 60;\n  const trackWidth = (dayEnd - dayStart) * pxPerMinute;\n  const hours = Array.from({ length: endHour - startHour + 1 }, (_, index) => startHour + index);\n\n  const [clock, setClock] = React.useState<number>(() => now ?? nowMinutes());\n  React.useEffect(() => {\n    if (now != null || !nowIndicator) return;\n    const timer = window.setInterval(() => setClock(nowMinutes()), 60_000);\n    return () => window.clearInterval(timer);\n  }, [now, nowIndicator]);\n  const currentMinutes = now ?? clock;\n\n  const byResource = React.useMemo(() => groupByResource(events, resources), [events, resources]);\n\n  return (\n    <div\n      ref={ref}\n      className={cn(\"pxui-swim\", className)}\n      style={\n        {\n          ...style,\n          [\"--pxui-swim-label\" as string]: `${labelWidth}px`,\n          [\"--pxui-swim-lane\" as string]: `${laneHeight}px`,\n          [\"--pxui-swim-hour\" as string]: `${60 * pxPerMinute}px`,\n        } as React.CSSProperties\n      }\n      {...rest}\n    >\n      <div className=\"pxui-swim-scroll\">\n        <div className=\"pxui-swim-inner\" style={{ width: labelWidth + trackWidth }}>\n          {/* Tijdas */}\n          <div className=\"pxui-swim-axis\">\n            <div className=\"pxui-swim-axis-label\" />\n            <div className=\"pxui-swim-axis-track\" style={{ width: trackWidth }}>\n              {hours.map((hour) => (\n                <span\n                  key={hour}\n                  className=\"pxui-swim-tick\"\n                  style={{ left: (hour * 60 - dayStart) * pxPerMinute }}\n                >\n                  {formatTime(hour * 60)}\n                </span>\n              ))}\n            </div>\n          </div>\n\n          {/* Banen */}\n          {resources.map((resource) => {\n            const list = byResource.get(resource.key) ?? [];\n\n            return (\n              <div className=\"pxui-swim-lane\" key={resource.key} data-today={resource.today ? \"\" : undefined}>\n                <div className=\"pxui-swim-label\">\n                  {resource.media && <span className=\"pxui-swim-media\">{resource.media}</span>}\n                  <span className=\"pxui-swim-label-text\">\n                    <span className=\"pxui-swim-name\">{resource.label}</span>\n                    <span className=\"pxui-swim-count\">\n                      {resource.sublabel ?? `${countBookable(list)} ${countLabel}`}\n                    </span>\n                  </span>\n                </div>\n\n                <div className=\"pxui-swim-track\" style={{ width: trackWidth }}>\n                  <div className=\"pxui-swim-lines\" aria-hidden=\"true\" />\n\n                  {resource.blocked && (\n                    <div className=\"pxui-swim-blocked-full\">\n                      {resource.blockedLabel && (\n                        <span className=\"pxui-swim-blocked-label\">{resource.blockedLabel}</span>\n                      )}\n                    </div>\n                  )}\n\n                  {onSlotClick && !resource.blocked && (\n                    <button\n                      type=\"button\"\n                      className=\"pxui-swim-slots\"\n                      aria-label={`Nieuw item bij ${String(resource.label)}`}\n                      onClick={(clickEvent) => {\n                        const rect = clickEvent.currentTarget.getBoundingClientRect();\n                        const minutes = dayStart + (clickEvent.clientX - rect.left) / pxPerMinute;\n                        onSlotClick(resource.key, Math.round(minutes / step) * step);\n                      }}\n                    />\n                  )}\n\n                  {list.length === 0 && !resource.blocked && (\n                    <span className=\"pxui-swim-empty\">{emptyLabel}</span>\n                  )}\n\n                  {list.map((event) => {\n                    const left = (Math.max(event.start, dayStart) - dayStart) * pxPerMinute;\n                    const width = Math.max(\n                      (Math.min(event.end, dayEnd) - Math.max(event.start, dayStart)) * pxPerMinute - 3,\n                      26\n                    );\n                    const tone = event.tone ?? \"accent\";\n\n                    return (\n                      <div\n                        key={event.id}\n                        role={onEventClick && !event.blocked ? \"button\" : undefined}\n                        tabIndex={onEventClick && !event.blocked ? 0 : undefined}\n                        className={cn(\n                          \"pxui-swim-appt\",\n                          event.blocked ? \"pxui-swim-appt-blocked\" : `pxui-swim-appt-${tone}`\n                        )}\n                        style={{\n                          left,\n                          width,\n                          ...(event.color\n                            ? ({ [\"--pxui-event-color\" as string]: event.color } as React.CSSProperties)\n                            : {}),\n                        }}\n                        title={`${formatTime(event.start)} – ${formatTime(event.end)}`}\n                        onClick={() => !event.blocked && onEventClick?.(event)}\n                        onKeyDown={(keyEvent) => {\n                          if (keyEvent.key === \"Enter\" || keyEvent.key === \" \") {\n                            keyEvent.preventDefault();\n                            if (!event.blocked) onEventClick?.(event);\n                          }\n                        }}\n                      >\n                        {renderEvent ? (\n                          renderEvent(event)\n                        ) : (\n                          <>\n                            <span className=\"pxui-swim-appt-time\">{formatTime(event.start)}</span>\n                            <span className=\"pxui-swim-appt-title\">{event.title}</span>\n                          </>\n                        )}\n                      </div>\n                    );\n                  })}\n\n                  {nowIndicator && currentMinutes >= dayStart && currentMinutes <= dayEnd && (\n                    <div\n                      className=\"pxui-swim-now\"\n                      style={{ left: (currentMinutes - dayStart) * pxPerMinute }}\n                    />\n                  )}\n                </div>\n              </div>\n            );\n          })}\n        </div>\n      </div>\n    </div>\n  );\n});\n"
    },
    {
      "path": "components/swimlanes.css",
      "content": "/* ============================ Swimlanes ============================ */\n.pxui-swim {\n  --pxui-swim-label: 180px;\n  --pxui-swim-lane: 54px;\n  background: var(--surface);\n  border: 1px solid var(--border);\n  border-radius: var(--r-lg);\n  box-shadow: var(--sh-sm);\n  overflow: hidden;\n}\n.pxui-swim-scroll { overflow: auto; -webkit-overflow-scrolling: touch; }\n.pxui-swim-inner { min-width: 100%; }\n\n/* ---------- Tijdas ---------- */\n.pxui-swim-axis {\n  display: flex; position: sticky; top: 0; z-index: 3;\n  background: var(--surface-2); border-bottom: 1px solid var(--border);\n  height: 30px;\n}\n.pxui-swim-axis-label {\n  width: var(--pxui-swim-label); flex: none;\n  border-right: 1px solid var(--border);\n  position: sticky; left: 0; z-index: 1; background: var(--surface-2);\n}\n.pxui-swim-axis-track { position: relative; flex: none; }\n.pxui-swim-tick {\n  position: absolute; top: 8px;\n  font-size: 10.5px; font-weight: 650; color: var(--text-3);\n  font-variant-numeric: tabular-nums; white-space: nowrap;\n  padding-left: 5px; border-left: 1px solid var(--border);\n  line-height: 1.2;\n}\n\n/* ---------- Baan ---------- */\n.pxui-swim-lane {\n  display: flex; align-items: stretch;\n  min-height: var(--pxui-swim-lane);\n  border-bottom: 1px solid var(--border);\n}\n.pxui-swim-lane:last-child { border-bottom: none; }\n\n.pxui-swim-label {\n  width: var(--pxui-swim-label); flex: none;\n  display: flex; align-items: center; gap: 9px;\n  padding: 8px 12px;\n  border-right: 1px solid var(--border);\n  position: sticky; left: 0; z-index: 2; background: var(--surface);\n}\n.pxui-swim-lane[data-today] .pxui-swim-label { background: var(--accent-tint); }\n.pxui-swim-media { flex: none; display: flex; }\n.pxui-swim-label-text { display: flex; flex-direction: column; min-width: 0; }\n.pxui-swim-name {\n  font-size: 12.5px; font-weight: 650; color: var(--text);\n  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;\n}\n.pxui-swim-count { font-size: 11px; color: var(--text-3); }\n\n/* ---------- Spoor ---------- */\n.pxui-swim-track { position: relative; flex: none; min-height: var(--pxui-swim-lane); }\n.pxui-swim-lines {\n  position: absolute; inset: 0; pointer-events: none;\n  background-image: repeating-linear-gradient(\n    to right,\n    var(--border) 0,\n    var(--border) 1px,\n    transparent 1px,\n    transparent var(--pxui-swim-hour, 144px)\n  );\n  opacity: .5;\n}\n.pxui-swim-slots {\n  position: absolute; inset: 0; width: 100%; height: 100%;\n  border: none; background: transparent; padding: 0; cursor: copy;\n}\n.pxui-swim-slots:hover { background: color-mix(in srgb, var(--accent) 5%, transparent); }\n.pxui-swim-slots:focus-visible { outline: none; box-shadow: inset var(--ring); }\n\n.pxui-swim-empty {\n  position: absolute; left: 14px; top: 50%; transform: translateY(-50%);\n  font-size: 12px; color: var(--text-3); font-style: italic; pointer-events: none;\n}\n\n.pxui-swim-blocked-full {\n  position: absolute; inset: 0; display: grid; place-items: center;\n  background-image: repeating-linear-gradient(\n    -45deg,\n    var(--surface-2) 0,\n    var(--surface-2) 5px,\n    var(--surface-3) 5px,\n    var(--surface-3) 10px\n  );\n}\n.pxui-swim-blocked-label {\n  font-size: 10.5px; font-weight: 650; text-transform: uppercase; letter-spacing: .05em;\n  color: var(--text-3); background: var(--surface);\n  border: 1px solid var(--border); border-radius: var(--r-full); padding: 2px 9px;\n}\n\n/* ---------- Blok ---------- */\n.pxui-swim-appt {\n  --pxui-event-color: var(--accent);\n  position: absolute; top: 7px; bottom: 7px;\n  display: flex; flex-direction: column; justify-content: center; gap: 1px;\n  padding: 4px 8px; overflow: hidden;\n  border-radius: var(--r-xs);\n  border: 1px solid color-mix(in srgb, var(--pxui-event-color) 32%, transparent);\n  border-left: 3px solid var(--pxui-event-color);\n  background: color-mix(in srgb, var(--pxui-event-color) 13%, var(--surface));\n  cursor: pointer; z-index: 1;\n  transition: box-shadow var(--dur-fast), transform var(--dur-fast);\n}\n.pxui-swim-appt:hover { box-shadow: var(--sh-md); z-index: 2; }\n.pxui-swim-appt:focus-visible { outline: none; box-shadow: var(--ring); z-index: 3; }\n.pxui-swim-appt-time {\n  font-size: 10px; font-weight: 700; color: var(--pxui-event-color);\n  font-variant-numeric: tabular-nums; line-height: 1.2;\n}\n.pxui-swim-appt-title {\n  font-size: 11.5px; font-weight: 600; color: var(--text); line-height: 1.25;\n  overflow: hidden; text-overflow: ellipsis; white-space: nowrap;\n}\n\n.pxui-swim-appt-accent { --pxui-event-color: var(--accent); }\n.pxui-swim-appt-green { --pxui-event-color: var(--green); }\n.pxui-swim-appt-amber { --pxui-event-color: var(--amber); }\n.pxui-swim-appt-red { --pxui-event-color: var(--red); }\n.pxui-swim-appt-blue { --pxui-event-color: var(--blue); }\n.pxui-swim-appt-violet { --pxui-event-color: var(--violet); }\n.pxui-swim-appt-neutral { --pxui-event-color: var(--text-3); }\n\n.pxui-swim-appt-blocked {\n  --pxui-event-color: var(--red);\n  cursor: default;\n  background-image: repeating-linear-gradient(\n    -45deg,\n    var(--red-tint) 0,\n    var(--red-tint) 4px,\n    var(--surface) 4px,\n    var(--surface) 9px\n  );\n}\n.pxui-swim-appt-blocked .pxui-swim-appt-title { color: var(--red); }\n.pxui-swim-appt-blocked:hover { box-shadow: none; }\n\n/* ---------- Nu-lijn ---------- */\n.pxui-swim-now {\n  position: absolute; top: 0; bottom: 0; width: 2px;\n  background: var(--red); z-index: 3; pointer-events: none;\n}\n.pxui-swim-now::before {\n  content: \"\"; position: absolute; top: -3px; left: -3px;\n  width: 8px; height: 8px; border-radius: var(--r-full); background: var(--red);\n}\n/* Alleen de bovenste baan krijgt het bolletje; de lijn loopt gewoon door. */\n.pxui-swim-lane + .pxui-swim-lane .pxui-swim-now::before { display: none; }\n"
    },
    {
      "path": "lib/schedule.ts",
      "content": "import type * as React from \"react\";\n\n/**\n * Gedeeld datamodel voor alle agendaweergaven.\n *\n * WeekSchedule, ResourceColumns, Swimlanes en TimeSlotList werken op precies\n * dezelfde gegevens, zodat je met één knop van weergave kunt wisselen zonder\n * je data om te bouwen.\n */\n\nexport type ScheduleTone = \"accent\" | \"green\" | \"amber\" | \"red\" | \"blue\" | \"violet\" | \"neutral\";\n\n/** Een kolom of rij in de agenda: een dag, een arts, een kamer, een machine. */\nexport interface ScheduleResource {\n  /** Unieke sleutel; events verwijzen hiernaar. */\n  key: string;\n  label: React.ReactNode;\n  /** Tweede regel, bv. het dagnummer of het aantal afspraken. */\n  sublabel?: React.ReactNode;\n  /** Avatar of icoon links van het label. */\n  media?: React.ReactNode;\n  /** Eigen kleur voor deze resource (koptekst, tag, blokken zonder eigen kleur). */\n  color?: string;\n  /** Markeert deze kolom als vandaag. */\n  today?: boolean;\n  /** Hele kolom gearceerd, bv. een sluitingsdag of afwezigheid. */\n  blocked?: boolean;\n  blockedLabel?: React.ReactNode;\n}\n\nexport interface ScheduleEvent {\n  id: string;\n  /** Sleutel van de resource (dag, arts, kamer …) waar dit item bij hoort. */\n  resource: string;\n  /** Begin in minuten sinds middernacht (09:30 = 570). */\n  start: number;\n  /** Einde in minuten sinds middernacht. */\n  end: number;\n  title: React.ReactNode;\n  subtitle?: React.ReactNode;\n  tone?: ScheduleTone;\n  /** Eigen kleur; overschrijft `tone` en de kleur van de resource. */\n  color?: string;\n  /** Kan niet versleept of aangepast worden. */\n  locked?: boolean;\n  /** Geblokkeerde tijd: gearceerd, nooit versleepbaar, geen klikactie. */\n  blocked?: boolean;\n  /** Vrij veld voor je eigen gegevens. */\n  meta?: unknown;\n}\n\n/** Positie van een event wanneer overlappende items naast elkaar staan. */\nexport interface LaidOutEvent<T extends { start: number; end: number }> {\n  event: T;\n  /** Kolomindex binnen de groep overlappende items. */\n  lane: number;\n  /** Aantal kolommen in die groep. */\n  lanes: number;\n}\n\n/**\n * Legt overlappende items naast elkaar: items die elkaar raken vormen een\n * cluster en krijgen daarbinnen elk een eigen kolom.\n */\nexport function packLanes<T extends { start: number; end: number }>(items: T[]): Array<LaidOutEvent<T>> {\n  const sorted = [...items].sort((a, b) => a.start - b.start || a.end - b.end);\n  const result: Array<LaidOutEvent<T>> = [];\n\n  let cluster: T[] = [];\n  let clusterEnd = -1;\n\n  const flush = () => {\n    if (cluster.length === 0) return;\n    const laneEnds: number[] = [];\n    const placed = cluster.map((event) => {\n      let lane = laneEnds.findIndex((end) => end <= event.start);\n      if (lane === -1) {\n        lane = laneEnds.length;\n        laneEnds.push(event.end);\n      } else {\n        laneEnds[lane] = event.end;\n      }\n      return { event, lane };\n    });\n    for (const entry of placed) result.push({ ...entry, lanes: laneEnds.length });\n    cluster = [];\n    clusterEnd = -1;\n  };\n\n  for (const event of sorted) {\n    if (cluster.length > 0 && event.start >= clusterEnd) flush();\n    cluster.push(event);\n    clusterEnd = Math.max(clusterEnd, event.end);\n  }\n  flush();\n\n  return result;\n}\n\n/** Groepeert events per resource, in de volgorde van de resources zelf. */\nexport function groupByResource<T extends { resource: string }>(\n  events: T[],\n  resources: Array<{ key: string }>\n): Map<string, T[]> {\n  const map = new Map<string, T[]>();\n  for (const resource of resources) map.set(resource.key, []);\n  for (const event of events) {\n    const list = map.get(event.resource);\n    if (list) list.push(event);\n  }\n  for (const list of map.values()) {\n    list.sort((a, b) => ((a as { start?: number }).start ?? 0) - ((b as { start?: number }).start ?? 0));\n  }\n  return map;\n}\n\n/** Groepeert events per starttijd, oplopend — de basis voor een tijdlijn. */\nexport function groupByStart<T extends { start: number }>(events: T[]): Array<{ start: number; events: T[] }> {\n  const map = new Map<number, T[]>();\n  for (const event of events) {\n    const list = map.get(event.start) ?? [];\n    list.push(event);\n    map.set(event.start, list);\n  }\n  return [...map.entries()]\n    .sort((a, b) => a[0] - b[0])\n    .map(([start, list]) => ({ start, events: list }));\n}\n\n/** Telt de items die echt een afspraak zijn (geblokkeerde tijd niet meegerekend). */\nexport function countBookable(events: Array<{ blocked?: boolean }>): number {\n  return events.filter((event) => !event.blocked).length;\n}\n\n/** De kleur van een event: eigen kleur, anders die van de resource, anders de toon. */\nexport function eventColor(\n  event: { color?: string; tone?: ScheduleTone },\n  resource?: { color?: string }\n): string | undefined {\n  return event.color ?? resource?.color;\n}\n\n/** Huidige tijd in minuten sinds middernacht. */\nexport function nowMinutes(): number {\n  const date = new Date();\n  return date.getHours() * 60 + date.getMinutes();\n}\n"
    }
  ]
}
